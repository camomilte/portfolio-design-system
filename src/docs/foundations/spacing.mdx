import { Meta } from '@storybook/addon-docs/blocks';
import tokens from '../../tokens/primitives/spacing.json';

<Meta title="Foundations/Spacing" />

# Spacing tokens

The following are the spacing tokens that form the basis of our spacing system.

## Spacing values

{Object.entries(tokens.space).map(([name, token]) => (

  <div key={name} style={{ 
    display: 'flex', 
    alignItems: 'center', 
    marginBottom: '16px' 
  }}>
    <div
      style={{
        width: token.value,
        height: '100px',
        backgroundColor: '#aeb9fe',
        borderRadius: '4px',
        marginRight: '24px',
      }}
    />
    <span>
      <span style={{ fontWeight: 700 }}>{name}</span>
      <br /> {token.value}
    </span>
  </div>
))}
