import { Meta, ColorPalette, ColorItem } from '@storybook/addon-docs/blocks';
import { colors } from './colors.ts';
import { background } from 'storybook/theming';
import { backgroundColors, interactiveElemenetColors, strokeColors, textColors } from './color.adapter.ts';

<Meta title="Foundations/Colors" />

# Colors

## Primitive Colors
Below are all the colors used in the design system, organized by their respective color families.

<ColorPalette>
  {Object.entries(colors)
  .map(([name, value]) => (
    <ColorItem key={name} title={name} colors={value} />
  ))}
</ColorPalette>

## Semantic Colors
Below are the semantic colors used in the design system, categorized by their usage.

### Background Colors
<ColorPalette>
  {backgroundColors.map(({ title, subtitle, colors }) => (
    <ColorItem 
      key={title} 
      title={title} 
      subtitle={subtitle} 
      colors={colors} 
    />
  ))}
</ColorPalette>

### Interactive Elements Colors
<ColorPalette>
  {interactiveElemenetColors.map(({ title, subtitle, colors }) => (
    <ColorItem
      key={title}
      title={title}
      subtitle={subtitle}
      colors={colors}
    />
  ))}
</ColorPalette>

### Text Colors
<ColorPalette>
  {textColors.map(({ title, subtitle, colors }) => (
    <ColorItem
      key={title}
      title={title}
      subtitle={subtitle}
      colors={colors}
    />
  ))}
</ColorPalette>

### Stroke Colors
<ColorPalette>
  {strokeColors.map(({ title, subtitle, colors }) => (
    <ColorItem
      key={title}
      title={title}
      subtitle={subtitle}
      colors={colors}
    />
  ))}
</ColorPalette>